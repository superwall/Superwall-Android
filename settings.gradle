pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "com.superwall.sdk"
include ':app'
include ':superwall'
include 'example:app'
project(':example:app').projectDir = new File('example/app')
include ':superwall-compose'
